<?xml version="1.0" encoding="UTF-8"?>
<fig id="Fig6">
 <label>Figure 6</label>
 <caption>
  <p>Total GND density plotted on YZ, XZ and the XY cross-sections through the indent centre. GND density was calculated using L2 optimisation. Predictions from CPFE calculations are shown in (
   <bold>a</bold>) and (
   <bold>b</bold>) for the unimplanted sample, and (
   <bold>e</bold>) and (
   <bold>f</bold>) for the helium-implanted sample. GND density computed from Laue diffraction measurements for the unimplanted and implanted samples are shown in (
   <bold>c</bold>)
   <sup>
    <xref ref-type="bibr" rid="CR34">34</xref>
   </sup> and (
   <bold>g</bold>) respectively. GND density determined by HR-EBSD for the unimplanted and the implanted sample is shown in (
   <bold>d</bold>) and (
   <bold>h</bold>) respectively. The same colour scale is used for all plots showing log
   <sub>10</sub>(ρ) with ρ in 1/µm
   <sup>2</sup>. (
   <bold>a</bold>), (
   <bold>c</bold>), (
   <bold>e</bold>) and (
   <bold>g</bold>) are plotted on the same length scale. (
   <bold>b</bold>), (
   <bold>d</bold>), (
   <bold>f</bold>) and (
   <bold>h</bold>) show magnified plots of GND density and are also all shown on the same length scale.
  </p>
 </caption>
 <graphic xlink:href="41598_2019_54753_Fig6_HTML" id="d29e2271" xmlns:xlink="http://www.w3.org/1999/xlink"/>
</fig>
